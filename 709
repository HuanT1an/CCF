char * toLowerCase(char * str){
int i=0;
while (str[i]!='\0')
    {
    if (str[i]>='A'&&str[i]<='Z') //判断是否为大写字母
    {
        str[i]=str[i]+'a'-'A';//字符串加减
    }
        ++i;
    }  
    return str;
}
